{% extends 'base.html.twig' %}

	{% block body %}
	<main class="container">
	<h1>Ajout d'une Tag</h1>
	
	{{ form_start(form) }}
		{{ form_widget(form) }}
		<input type="submit" value="valider" class="btn btn-outline-info">
	{{ form_end(form) }}
	
	</main>
	{% endblock %}